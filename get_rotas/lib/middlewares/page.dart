import 'package:flutter/material.dart';

class PageMiddleware extends StatelessWidget {
  const PageMiddleware({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
