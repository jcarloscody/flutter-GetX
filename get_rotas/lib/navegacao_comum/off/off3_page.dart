import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Off3Page extends StatelessWidget {
  const Off3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('3'),
      ),
      body: Container(),
    );
  }
}
