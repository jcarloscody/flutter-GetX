import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Off3AllPage extends StatelessWidget {
  const Off3AllPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('3'),
      ),
      body: Container(),
    );
  }
}
